sudo: false
language: python
matrix:
  include:
  - python: 2.7
    env: TOX_ENV=flake8
  - python: 2.7
    env: TOX_ENV=py27
  - python: 3.6
    env: TOX_ENV=py36
dist: bionic
addons:
  apt:
    packages:
      libgit2-dev
      libgit2-26
install:
- pip install tox
script:
- tox -e $TOX_ENV
